# 🧩 KYC Onboarding Module — Full Stack Assessment

**Vue 3 + FastAPI + MongoDB**

This project implements a minimal KYC onboarding module for internal back-office use.
Operators can create verification requests, review them, update their status, and view risk scoring generated by the backend.

---

# 🚀 Instrucciones para ejecutar el proyecto

## 1. Backend (FastAPI)

### Instalar dependencias

```bash
cd backend
python -m venv .venv
.\.venv\Scripts\activate     # Windows
pip install -r requirements.txt
```

### Configurar MongoDB

En la consola de Mongo:

```js
use sasskyc
db.createUser({
  user: "sasskyc",
  pwd: "CHANGE_THIS_PASSWORD!!!",
  roles: [{ role: "readWrite", db: "sasskyc" }]
})
```

Crear archivo `backend/.env`:

```
MONGODB_URI=mongodb://sasskyc:CHANGE_THIS_PASSWORD!!!@127.0.0.1:27017/sasskyc?authSource=sasskyc
MONGODB_DB_NAME=sasskyc
```

### Probar conexión a base de datos

```bash
python tests/test_mongo.py
```

### Iniciar servidor FastAPI

```bash
python -m uvicorn app.main:app --reload --port 8000
```

Documentación Swagger:
[http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

---

## 2. Frontend (Vue 3 + Vite)

### Instalar dependencias

```bash
cd frontend
npm install
```

### Variables de entorno

Crear `frontend/.env`:

```
VITE_API_BASE_URL=http://localhost:8000
```

### Ejecutar servidor de desarrollo

```bash
npm run dev
```

Aplicación disponible en:
[http://localhost:5173](http://localhost:5173)

---

# 🛠️ Tecnologías utilizadas

## Backend

* **FastAPI (Python)**
* **Motor (MongoDB async driver)**
* **Pydantic v2**
* **Uvicorn**
* **pytest** para pruebas

## Frontend

* **Vue 3 (Composition API)**
* **TypeScript**
* **Vite**
* **Tailwind CSS**
* **Vue Router**
* **Axios**
* **Vitest** o test runner equivalente

## Base de Datos

* **MongoDB** (local o cluster)

---

# 🧠 Decisiones técnicas clave

### ✔ Elección de MongoDB

* Esquema flexible para los datos de solicitudes KYC.
* Integración simple con Motor en FastAPI.
* Permite entregar rápidamente una solución funcional para el assessment.

### ✔ Arquitectura del backend

* Estructura basada en módulos:

  * `api/` → endpoints
  * `core/` → configuración y conexión DB
  * `schemas/` → modelos Pydantic
  * `services/` → lógica de negocio y motor de riesgo

### ✔ Motor de riesgo separado

* Calcula un `score` basado en reglas simples:

  * Dominios de correo riesgosos
  * Países restringidos
  * Longitud mínima del número de documento
* Produce un nivel: **low**, **medium**, **high**
* Testeable de forma independiente.

### ✔ Frontend con Vue 3 + Vite

* Desarrollo rápido y ligero.
* Tailwind para estilizar de manera eficiente.
* Axios configurado con base URL mediante variables de entorno.

---

# ⚠️ Limitaciones conocidas

* No incluye autenticación para operadores (UI abierta por simplicidad).
* Validaciones básicas en el formulario (solo lo requerido por la prueba).
* No hay paginación ni ordenamiento en el listado.
* Las imágenes de documentos se representan como URLs (no hay carga de archivos).
* Motor de riesgo simple (reglas estáticas).

---

# 🧪 Cómo ejecutar las pruebas automatizadas

## Backend tests

```bash
cd backend
pytest
```

Incluye pruebas para:

* Motor de riesgo
* Construcción del documento de solicitud

## Frontend tests

```bash
cd frontend
npm run test
```

Incluye al menos:

* Prueba de un componente o función utilitaria
